# Habit Tracker 작업 지침

초안 v1 · 2026-09-05

이 파일은 프로젝트 전체의 화면 추가, 기능 수정, 리팩토링에 적용한다. 비용 최적화를 우선하며 요청에 필요한 최소 변경으로 작업한다. 초안이지만 별도 변경 요청 전까지 아래 필수 규칙을 따른다.

## 1. 판단 우선순위

규칙이 충돌하면 다음 순서로 판단한다.

1. 사용자의 현재 요청
2. 티켓·기획서·API·DB 명세 등 공식 요구사항
3. 현재 프로젝트의 기존 코드 패턴과 팀 규칙
4. 이 문서의 지침
5. 일반적인 Kotlin·Android 모범 사례

- README의 복권 생성 설계 원칙을 해당 도메인 작업 시 함께 읽는다.
- 새 화면의 디자인 기준은 README의 `화면 디자인 기준`, 공통 컴포넌트, 테마를 따른다. 개별 화면에 남은 과거 스타일을 새로운 표준으로 복제하지 않는다.
- 불명확한 사항은 구현을 막는 사항과 후속 확인 사항으로 구분한다. 요구사항에 없는 정책·상태·기본값·필드·DB 컬럼·정렬 조건을 임의로 추가하지 않는다.

## 2. 작업 범위와 실행 제한

- 관련 파일만 읽고 수정한다. 전체 프로젝트 스캔, 무관한 포맷팅·리네임·정리, 대규모 리팩토링을 하지 않는다.
- 파일·문자열 검색은 필요한 디렉터리를 지정한 `rg`를 우선한다. 로그는 관련 오류의 최소 범위만 읽는다.
- 기존 미커밋 변경을 보존한다. 사용자가 작성한 내용을 덮어쓰거나 되돌리지 않는다.
- 사용자가 명시적으로 요청하지 않으면 앱·스크립트 실행, 컴파일, 테스트, 빌드, 패키징, 설치를 하지 않는다. 코드 수정 후 자동 검증 명령도 실행하지 않는다.
- 파일 읽기와 요청된 편집은 진행하되, 새 파일·의존성·플러그인·빌드 설정·코드 생성 도구는 필요한 경우에만 사용한다. 현재 요청에 포함되지 않은 도입은 먼저 승인받는다.
- 권한이 필요한 작업은 승인 후 진행한다. 이미 승인된 동일 범위를 반복 확인하지 않는다.
- 같은 작업을 불필요하게 반복하지 않는다. 실패하면 원인과 다음 조치를 설명하고, 원인이 해소되지 않은 채 재시도하지 않는다.
- 검증이 필요하지만 실행 승인이 없으면 사용자가 직접 실행할 최소 명령만 제안한다. 전체 테스트는 실행하지 않고 승인된 특정 범위만 다룬다.

## 3. Android 구조와 책임

- Kotlin, Jetpack Compose, Material 3, ViewModel·StateFlow, Room의 기존 구성을 유지한다.
- Screen은 표시와 사용자 입력에 집중한다. DB·네트워크 호출이나 무거운 계산을 Composable에 넣지 않는다.
- ViewModel은 화면 상태와 유스케이스 흐름을 관리한다. UI에 읽기 전용 상태를 노출하고 기존 `AppViewModelFactory` 연결을 따른다.
- Repository는 데이터 접근 경계를 유지한다. 복잡한 계산·저장 흐름의 분리는 현재 중복이나 책임 집중을 해결하는 경우에만 한다.
- `DailyRecordStore`처럼 이미 분리된 책임을 다시 `HabitRepository`에 넣지 않는다. 기능 하나를 위해 새 DI 프레임워크나 불필요한 인터페이스를 만들지 않는다.
- 사용자 메시지는 한국어로 작성한다. 주석은 필요한 이유와 제약을 설명하고 코드 동작을 반복 설명하지 않는다.

## 4. 화면 디자인 필수 규칙

- 스타일은 모던·미니멀·프리미엄으로 유지하되, 정보 계층과 가독성을 장식보다 우선한다.
- 밝은 중립 배경, 짙은 본문, 절제된 청록 포인트를 기본으로 한다. 색·글꼴·모서리·여백은 테마와 공통 토큰을 사용한다.
- 일반 화면에 그라데이션, 유리 효과, 큰 장식 이미지, 입장 애니메이션, 과도한 그림자를 추가하지 않는다.
- 모든 정보를 둥근 카드로 감싸지 않는다. 일반 정보는 제목·여백·구분선·목록으로 나누고, 카드가 필요한 독립 정보에만 제한적으로 사용한다.
- 화면마다 별도 버튼·입력창·필터 스타일을 만들지 않는다. 공통 기능이 부족하면 기존 호출부 호환성을 유지하며 공통 컴포넌트를 보완한다.
- 트렌드를 이유로 실험 API·라이브러리 업그레이드를 자동 도입하지 않는다. 최신 사례 조사 요청이 있으면 공식 문서와 실제 서비스 자료를 확인하고, 확인한 시점과 출처를 남긴다.

| 역할 | 사용 기준 |
| --- | --- |
| 화면·스크롤·키보드 여백 | `AppScreen` |
| 화면 제목·짧은 설명 | `AppHeroCard` — 이름과 달리 장식형 히어로 카드를 다시 만들지 않음 |
| 일반 정보 묶음·제목 | `AppSectionCard`, `AppSectionHeader` |
| 주요·보조 행동 | `AppPrimaryButton`, `AppSecondaryButton` |
| 저장·수정 | `AppSaveButton`, `AppEditButton` |
| 입력 | `AppTextField`, `AppOutlinedTextField` |
| 선택 필터 | `AppSelectableChip` |
| 결과 안내 | `AppActionNotice`와 앱 공통 Snackbar |
| 사용자 확인 | `AppConfirmDialog` |
| 로딩·빈 상태 | `AppLoadingCard`, `AppEmptyCard` |

## 5. 탐색과 정보 계층

- 하단 탐색은 홈·기록·통계·전체를 유지한다. 새 기능은 적절한 기존 진입점이나 전체 메뉴에 연결하고, 하단 항목을 임의로 늘리지 않는다.
- 경로는 `AppDestination`, 화면 연결은 `MainActivity`의 기존 NavHost 구조를 따른다. 상세 화면에서도 상위 메뉴의 선택 상태를 유지한다.
- 뒤로 가기는 공통 탐색과 일관되게 처리한다. 같은 화면에 동일한 뒤로 가기 버튼을 중복 배치하지 않는다.
- 사용자가 가장 자주 수행하는 행동과 핵심 정보를 먼저 보여준다. 큰 소개 영역 때문에 입력·조회·저장 동작이 밀리지 않게 한다.
- 아무 동작도 하지 않는 버튼을 만들지 않는다. 현재 위치나 선택 상태는 제목·탭·필터 상태로 표현한다.
- 검색 결과 없음, 저장 데이터 없음, 로딩, 실패를 구분한다. 실패를 빈 목록이나 정상 완료처럼 표시하지 않는다.

## 6. 입력과 실수 방지

- 날짜·탭·화면 전환과 시스템 뒤로 가기로 편집 내용이 사라질 수 있으면 미저장 변경을 감지하고 기존 `LocalAppNavigationGuard` 흐름을 사용한다.
- 입력 초안은 화면 재생성에도 유지한다. 데이터 크기와 성격에 맞게 `rememberSaveable` 또는 `SavedStateHandle`을 사용하고, 비밀번호·토큰을 평문 저장 상태에 넣지 않는다.
- DB 재방출이나 상태 메시지 변경만으로 사용자가 편집 중인 값을 초기화하지 않는다.
- 긴 입력 화면의 주요 저장 행동은 접근하기 쉽게 유지한다. 고정 저장 영역은 `AppScreen`의 `bottomBar`를 사용하고 키보드·시스템 영역과 겹치지 않게 한다.
- 저장·생성·주문 등 중복 실행이 문제가 되는 행동에는 진행 상태와 중복 실행 방지를 연결한다. UI 비활성화만으로 데이터 정합성을 보장하지 않는다.
- 숫자·시간·비밀번호는 적절한 키보드와 입력 표시를 사용한다. 파싱 실패를 조용히 0이나 빈 값으로 저장하지 않는다.
- 입력 오류는 해당 필드 가까이에 원인과 수정 방법을 제공한다. 오류를 색상만으로 표시하지 않는다.
- 복원이 어려운 삭제는 대상이 보이는 확인 단계를 제공한다. 성공 알림만으로 삭제 전 확인을 대신하지 않는다.
- 일반 작업 성공은 Snackbar로 알린다. 주문 전 확인 등 기존의 명시적인 판단 단계는 디자인 변경을 이유로 제거하지 않는다.

## 7. 접근성과 다양한 화면 크기

- 버튼·선택 항목에는 충분한 터치 영역을 확보하고, 큰 글꼴에서도 문구가 잘리지 않도록 고정 높이를 남발하지 않는다.
- 작은 화면·가로 화면·큰 글꼴·키보드 표시를 고려한다. 고정 폭이 필요한 차트나 표는 스크롤 등 접근 수단을 제공한다.
- 중요한 금액·날짜·행동 이름을 말줄임으로 숨기지 않는다. 보조 정보 축약은 전체 내용을 확인할 수 있는 경로가 있을 때만 적용한다.
- 제목, 선택 상태, 의미 있는 아이콘, 달력의 날짜·기록 상태에 접근성 정보를 제공한다. 장식 이미지에는 중복 설명을 넣지 않는다.
- 상태를 색상만으로 구분하지 않는다. 텍스트·기호와 함께 표현하며 기존 대비 수준을 낮추지 않는다.
- 해당 기기 검증은 실행 승인이 있을 때 수행하고, 미실행이면 완료 보고에 명시한다.

## 8. 성능과 비동기 처리

- Composable 재구성이나 입력 한 글자 변경마다 전체 통계를 다시 계산하지 않는다. 분석 데이터와 입력·알림 상태를 분리한다.
- 계산은 실제 입력 데이터가 바뀔 때만 수행한다. 무거운 CPU 계산은 `Dispatchers.Default`, 블로킹 I/O는 `Dispatchers.IO`를 사용한다.
- 메인 스레드에서 DB 열기·복원·파일 처리·네트워크 호출을 하지 않는다. 초기 저장소 준비는 `AppContainer`의 준비 상태와 `awaitRepository()`를 따른다.
- 조회 Flow는 해당 조건에만 연결한다. 선택 날짜 변경 때문에 월간 전체 조회를 다시 구독하지 않는다.
- 화면 수집은 `collectAsStateWithLifecycle`을 사용하고, 지속 구독이 필요한 이유가 없으면 화면 밖 작업을 줄인다.
- 정렬된 DAO 결과를 다시 정렬하지 않는다. 반복 검색은 필요에 따라 맵을 재사용하고, 반복문 안의 `drop(...).take(...)` 같은 큰 임시 목록 복사를 피한다.
- 큰 목록에는 지연 구성과 안정적인 키를 사용한다. 기존 페이지 조회나 상한을 임의로 제거하지 않는다.
- 코루틴 취소를 일반 실패로 삼키지 않는다. 캐시를 추가하면 갱신 조건과 오래된 결과가 표시될 가능성을 함께 다룬다.
- 성능을 측정하지 않았다면 개선 후보·구조적 감소로 설명한다. 속도 향상 수치를 추정해 단정하지 않는다.

## 9. 데이터와 도메인 보호

- 저장 트랜잭션, 기존 마이그레이션, 백업 요청 흐름을 유지한다. 편의를 위해 파괴적 마이그레이션이나 데이터 초기화를 추가하지 않는다.
- 중복 생성과 단일 실행 보장은 기존 DB 제약·트랜잭션을 함께 고려한다. 단순 조회 후 삽입이나 버튼 잠금만으로 보장한다고 설명하지 않는다.
- 외부 API 호출과 장시간 I/O를 DB 트랜잭션 안에 넣지 않는다.
- 실전 주식 주문·자동매매의 안전 한도, 차단 조건, 주문 확인 절차를 UI 작업에 섞어 변경하지 않는다. 실전 주문을 검증 수단으로 사용하지 않는다.
- 복권 생성의 필수 조건·점수·분산 정책은 README 기준을 유지한다. 추천 점수를 당첨 확률 상승으로 표현하지 않는다.
- 실패·재시도·상태 전이 거부는 기존 로그 패턴에 맞춰 원인과 필요한 식별자를 남긴다. 원본 예외를 보존하고 토큰·비밀번호·개인정보·전체 민감 payload는 기록하지 않는다.

## 10. 완료 보고와 Git

- 변경 내용, 변경하지 않은 범위, 남은 확인 사항을 간결하게 구분한다.
- 실행하지 않은 테스트·빌드·실기기 검증은 실행하지 않았다고 명시한다. 검증 없이 `production-ready`, 회귀 없음, 성능 향상 확정으로 보고하지 않는다.
- 공통 컴포넌트 수정 시 영향받는 화면을 설명한다. 새 탐색이나 사용자 동작을 바꾸면 README의 화면 기준도 필요한 범위에서 갱신한다.
- 커밋·푸시는 명시적 요청이 있을 때만 한다. 커밋 메시지와 PR 제목·설명은 한국어로 작성한다. Conventional Commits의 타입·scope는 영문을 유지할 수 있다.

### 새 화면 작업 완료 전 체크리스트

아래는 소스 검토 기준이며, 자동 실행·빌드·테스트 승인을 의미하지 않는다.

- [ ] 기존 공통 컴포넌트·테마·탐색 구조를 사용했다.
- [ ] 핵심 정보와 주요 행동을 먼저 배치했다.
- [ ] 해당하는 로딩·빈 결과·검색 결과 없음·실패 상태를 구분했다.
- [ ] 해당하는 입력 복원·미저장 이탈·삭제 확인·중복 실행 방지를 연결했다.
- [ ] 큰 글꼴·작은 화면·키보드·접근성에 불리한 고정 크기나 색상 전용 표시가 없다.
- [ ] 재구성·상태 메시지 때문에 불필요한 조회나 전체 계산을 반복하지 않는다.
- [ ] 기존 데이터·백업·주문·복권 정책을 보존했다.
- [ ] 승인된 실행 범위를 지켰으며 미검증 항목을 완료 보고에 남겼다.
